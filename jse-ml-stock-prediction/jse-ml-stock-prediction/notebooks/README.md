This folder can be used for quick experiments in Jupyter Notebook.

A simple way to extend the project is to copy the steps from `src/train.py` into a notebook and then test:
- different JSE tickers,
- different rolling window sizes,
- extra technical indicators,
- classification instead of regression.
